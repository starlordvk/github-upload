#include <bits/stdc++.h>

using namespace std;

class SetOfStacks
{
    queue<stack<int>> queueOfStacks;

    public:


    void push(int val)
    {
        stack<int> s = queueOfStacks.back();
        if (s != NULL && !s.empty())
        {
            s.self
        }
    }
};

int main()
{
     queue<stack<int>> q;

    return 0;

}